import { useState } from 'react'; import { useDispatch } from 'react-redux'; import { useNavigate } from 'react-router-dom'; import PageShell from '../components/PageShell'; import { login } from '../redux/slices/authSlice';
export default function Login(){ const [email,setEmail]=useState('admin@melodyrent.local'); const [password,setPassword]=useState('Password123!'); const dispatch=useDispatch(); const nav=useNavigate(); const submit=async(e)=>{e.preventDefault(); await dispatch(login({email,password})); nav('/dashboard')}; return <PageShell><form onSubmit={submit} className="glass mx-auto mt-16 max-w-md rounded-[2rem] p-8"><h1 className="text-3xl font-black">Welcome back</h1><input className="mt-6 w-full rounded-full px-5 py-3 text-ink" value={email} onChange={e=>setEmail(e.target.value)}/><input className="mt-3 w-full rounded-full px-5 py-3 text-ink" type="password" value={password} onChange={e=>setPassword(e.target.value)}/><button className="mt-6 w-full rounded-full bg-blush px-5 py-3 font-bold text-white">Login</button></form></PageShell> }
