import { AnimatePresence } from 'framer-motion'; import { useEffect } from 'react'; import { useDispatch } from 'react-redux'; import { useLocation } from 'react-router-dom'; import AppRoutes from './routes/AppRoutes.jsx'; import MainLayout from './layouts/MainLayout.jsx'; import { connectLiveUpdates } from './services/socket.js'; import { pushNotification } from './redux/slices/uiSlice.js';
export default function App(){ const location=useLocation(); const dispatch=useDispatch(); useEffect(()=>{ const c=connectLiveUpdates(null,(n)=>dispatch(pushNotification(n))); return()=>c.deactivate();},[dispatch]); return <MainLayout><AnimatePresence mode="wait"><AppRoutes key={location.pathname}/></AnimatePresence></MainLayout> }
